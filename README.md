# phase-linearizer

Depends on several Octave Forge packages:
- optim
    - struct
    - statistics
- control
- signal

Install using octaves pkg command:
```shell
octave
> pkg install -forge struct
> pkg install -forge statistics
> pkg install -forge optim
> pkg install -forge signal
> pkg install -forge control
```
